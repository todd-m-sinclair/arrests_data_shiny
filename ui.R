# UI elements
library(shiny)
library(shinydashboard)

dashboardPage(
  dashboardHeader(title = "1973 US Arrests with R & Shiny Dashboard",
                  titleWidth = 650),
  dashboardSidebar(),
  dashboardBody()
)